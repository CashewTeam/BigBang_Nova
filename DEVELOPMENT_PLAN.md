# 开发计划

## 项目定位

BigBang Nova 当前已经完成“可编译、可运行、可本地分词、可通过设置页和悬浮球进入主链路”的第一阶段，不再适合继续按最早的 P0/P1 迁移草稿推进。

后续计划应该从“补脚手架”切换为“补能力、补 feature、补兼容性”。

这份文档描述的是**当前真实状态**下的下一阶段开发重点，而不是最初的理想化迁移蓝图。

## 当前开发状态

### 已完成

#### 工程与基础链路

- Gradle 构建链已打通
- legacy `src/` / `res/` 已通过 `sourceSets` 接入
- `cppjieba` JNI 已接入，本地分词已替代远程分词主路径
- App 启动后会异步预热 jieba
- `BigBangSettings` 已接管搜索、词典、调试文本等应用内配置

#### 页面与交互

- 设置页已重构为 Compose
- 设置页已支持：
  - 权限状态检查
  - 悬浮球开关
  - 预制文本调试
  - 自定义调试文本输入
  - BigBang 页面预览
  - jieba 预热状态显示
  - 搜索 / 词典默认项切换
- 大爆炸页外层 UI 已改为 Compose 浮层壳
- 搜索页已改为 Compose + 原生 WebView 浮层页
- 大爆炸 / 搜索页已支持深色模式和多窗口全屏适配

#### 触发入口

- 悬浮球服务已接入
- 无障碍服务已接入
- `BigBangCaptureDispatcher` 已可将捕获文本传入 BigBang
- BigBang 拉起链路已统一为 `OverlayActivity -> BoomActivity`

### 未完成

- OCR 仍未完成迁移，旧 `BoomOcrActivity` 当前处于不可用状态
- 原版大爆炸 feature 仍不完整
- 三方 App 的文本捕获、页面覆盖、返回栈、性能问题仍需持续适配
- legacy Java BigBang 内核仍未完全 Kotlin 化

## 接下来的三项主目标

## 目标一：接入 Android 原生 ML Kit OCR

### 目标

用 ML Kit 替换当前不可用的 CamScanner OCR 路径，建立一条真正可用的本地 OCR 主链路。

### 为什么先做

- 当前 OCR 在产品层面基本不可用
- 远程分词已经切掉，OCR 是主能力缺口中最大的一个
- ML Kit 是当前阶段最现实的本地 Android 原生方案，集成成本和维护成本都低于继续保留旧商业 SDK

### 交付范围

- 接入 ML Kit Text Recognition
- OCR 结果能进入 BigBang 主链路
- 旧 CamScanner 逻辑彻底退场，不再作为运行时依赖

### 具体任务

- [ ] 在 `app/build.gradle.kts` 中接入 ML Kit OCR 依赖
- [ ] 新建最小 OCR 封装层
  - 只做单一职责：`Bitmap -> String`
  - 不提前抽象成多实现工厂
- [ ] 梳理现有 OCR 入口
  - `BoomOcrActivity`
  - 无障碍链路中的 OCR fallback 预留点
  - 截图 / 图片输入链路
- [ ] 将 `BoomOcrActivity` 改造成“截图输入 + OCR 调用 + 结果跳转 BigBang”的最小中转页
- [ ] 删掉运行时对 `csopensdk.jar` / `CSOpenAPI` 的依赖
- [ ] 在设置页或调试入口增加 OCR 调试路径

### 验收标准

- [ ] 无需 CamScanner，OCR 页面可以正常识别文本
- [ ] OCR 结果可直接进入 BigBang 页面
- [ ] OCR 失败时有明确反馈，不崩溃
- [ ] `BoomOcrActivity` 主路径不再被硬编码禁用

## 目标二：继续补齐大爆炸原版功能 feature

### 目标

把当前“能用的 BigBang”继续推进到“更接近原版 BigBang”的交互完成度，优先补齐最影响体验的缺口。

### 当前最缺的 feature

- 炸了又炸
- 编辑模式
- 更多原版细节交互和操作面板行为

### 具体任务

#### 2.1 炸了又炸

- [ ] 梳理原版“炸了又炸”的触发条件和状态机
- [ ] 明确当前 `BoomActionHandler` / `BoomChipPage` 中哪些逻辑可直接复用
- [ ] 在现有 BigBang 页内补上再次炸开的交互
- [ ] 做最小调试入口，避免只能靠悬浮球验证

#### 2.2 编辑模式

- [ ] 明确编辑模式的目标行为
  - 是否允许单词重新组合
  - 是否允许删除 / 合并 / 二次选择
  - 操作完成后如何回到普通 BigBang
- [ ] 先实现占位逻辑切换为真实状态切换
- [ ] 补 UI 反馈与退出路径

#### 2.3 原版交互补齐

- [ ] 补齐顶部 / 底部操作区的缺口行为
- [ ] 对齐选中态、清空态、空白点击、关闭逻辑
- [ ] 收口搜索 / 分享 / 复制等操作从 BigBang 到搜索页的过渡体验

### 验收标准

- [ ] “炸了又炸”可以真实使用
- [ ] 编辑模式不再只是占位按钮
- [ ] BigBang 主要交互不再依赖“先这样用”的临时兜底

## 目标三：三方应用适配与性能调优

### 目标

把当前“在部分场景可用”的主链路打磨到更稳定的日常可用状态，重点处理三方 App 兼容性和页面性能问题。

### 重点方向

#### 3.1 三方应用适配

- [ ] 无障碍文本提取命中率排查
- [ ] 悬浮球在不同 ROM / 多窗口 / 小窗模式下的行为检查
- [ ] BigBang / 搜索页在其他 App 上层显示时的返回栈和动画问题排查
- [ ] WebView 搜索页在常见站点的兼容性记录与修补
- [ ] 文本为空、文本过短、文本过长等边界场景处理

#### 3.2 性能调优

- [ ] 设置页 Compose 滚动与绘制性能继续收敛
- [ ] 大爆炸页外层 Compose 与内部 legacy View 的过绘检查
- [ ] 搜索页 WebView 启动时延与页面切换时延检查
- [ ] jieba 预热与首次分词耗时记录
- [ ] 悬浮球触发到 BigBang 展开的整链路耗时记录

#### 3.3 监测与验证

- [ ] 建立最小性能观察清单
  - 首次启动
  - 首次分词
  - 打开 BigBang
  - 打开搜索页
  - 设置页滚动
- [ ] 形成“问题机型 / 问题 App / 复现步骤”记录

### 验收标准

- [ ] 主流文本场景下悬浮球触发成功率可接受
- [ ] 设置页、大爆炸页、搜索页无明显掉帧和严重视觉 bug
- [ ] 三方 App 使用时返回栈、浮层显示、关闭逻辑基本稳定

## 当前不作为主目标的事项

以下内容不是不做，而是**不作为下一阶段主目标**：

- 全量 Kotlin 化 legacy Java 内核
- 完整 MVVM / ViewModel 分层重构
- 全量 Compose 重写所有 BigBang 内部词块逻辑
- 替换所有旧资源与动画实现
- 大规模目录重排

原因很简单：这些工作量大，但不直接补当前最缺的用户能力。

## 里程碑建议

### M1：ML Kit OCR 跑通

- OCR 可实际识别并跳转 BigBang
- CamScanner 路径退出主链路

### M2：原版核心 feature 补齐第一轮

- 炸了又炸可用
- 编辑模式可用

### M3：三方适配和性能第一轮收敛

- 悬浮球 / 无障碍 / 搜索页 / 设置页的主要体验问题收敛

## 执行原则

1. 继续保持“先让主链路可用，再补抽象”。
2. 不为了文档好看而把当前状态写成目标态。
3. 新功能优先接到现有可验证链路上，避免只加代码不加调试入口。
4. 对 legacy Java 内核做小步替换，不做一次性重写。
5. 性能问题优先找真实热区，不再为了视觉效果堆额外绘制层。
