# Nova Text 开发计划

## 项目定位

Nova Text 当前已经完成“可编译、可运行、可本地分词、可通过设置页和悬浮球进入主链路”的第一阶段，不再适合继续按最早的 P0/P1 迁移草稿推进。

后续计划应该从“补脚手架”切换为“补能力、补 feature、补兼容性”。

这份文档描述的是**当前真实状态**下的下一阶段开发重点，而不是最初的理想化迁移蓝图。

## 当前开发状态

### 已完成

#### 工程与基础链路

- Gradle 构建链已打通
- legacy `src/` / `res/` 已通过 `sourceSets` 接入
- `cppjieba` JNI 已接入，本地分词已替代远程分词主路径
- App 启动后会异步预热 jieba
- `BigBangSettings` 已接管搜索、词典、调试文本等应用内配置

#### 页面与交互

- 设置页已重构为 Compose
- 设置页已支持：
  - 权限状态检查
  - 悬浮球开关
  - 预制文本调试
  - 自定义调试文本输入
  - BigBang 页面预览
  - jieba 预热状态显示
  - 搜索 / 词典默认项切换
- 大爆炸页外层 UI 已改为 Compose 浮层壳
- 搜索页已改为 Compose + 原生 WebView 浮层页
- 大爆炸 / 搜索页已支持深色模式和多窗口全屏适配

#### 触发入口

- 悬浮球服务已接入
- 无障碍服务已接入
- `BigBangCaptureDispatcher` 已可将捕获文本传入 BigBang
- BigBang 拉起链路已统一为 `OverlayActivity -> BoomActivity`

### 未完成

- OCR 底层能力已经接入，`BoomOcrActivity` 已支持图片输入、区域框选、本地识别和结果进入 BigBang
- 原版大爆炸 feature 仍不完整
- 三方 App 的文本捕获、页面覆盖、返回栈、性能问题仍需持续适配
- legacy Java BigBang 内核仍未完全 Kotlin 化

## 接下来的三项主目标

## 目标一：补完 OCR 交互与多场景体验

### 目标

在已经接通的 ML Kit OCR 底层之上，把 OCR 从“可用”推进到“可在真实场景中稳定使用”。

### 当前真实状态

- ML Kit OCR V2 已接入
- `BoomOcrActivity` 已支持：
  - 设置页调试入口
  - 系统图片分享入口
  - 区域框选后识别
  - 识别结果进入 BigBang
- CamScanner 已退出主链路，不再是当前方向

### 还缺什么

- 还没有接入悬浮球交互链路
- 还没有做免区域框选识别
- 还没有做多场景自动裁切和场景优化
- OCR 页视觉和交互虽然已现代化，但还未完全对齐大爆炸整体体验

### 具体任务

- [ ] 把悬浮球链路正式接入 OCR
  - 明确无障碍抓不到文本时何时切 OCR
  - 明确截图输入契约和返回路径
- [ ] 设计并实现免区域框选识别
  - 优先定义默认策略，再决定是否保留手动框选回退
  - 不先做过度复杂的自动重试
- [ ] 做多场景裁切优化
  - 不同应用内容区域
  - 图片 / 网页 / 长截图 / 侧边栏场景
  - 多窗口 / 小窗 / 全屏差异
- [ ] 继续打磨 OCR Compose 页体验
  - 框选手感
  - 深色模式
  - 多窗口展示
  - 加载态和取消态
- [ ] 收口现有 OCR 调试入口和系统分享入口的行为
  - 避免两条入口在默认框选、返回路径和识别模式上继续分叉

### 验收标准

- [ ] 悬浮球场景下可进入 OCR 主链路
- [ ] 常见场景下不需要每次手动重新精细框选
- [ ] OCR 识别结果仍稳定进入 BigBang
- [ ] OCR 失败或取消时反馈明确，不崩溃

## 目标二：继续补齐大爆炸原版功能 feature

### 目标

把当前“能用的 BigBang”继续推进到“更接近原版 BigBang”的交互完成度，优先补齐最影响体验的缺口。

### 当前最缺的 feature

- 炸了又炸
- 编辑模式
- 更多原版细节交互和操作面板行为

### 具体任务

#### 2.1 炸了又炸

- [ ] 按正确产品定义补齐“炸了又炸”
  - 大爆炸识别一段文本后
  - 用户上滑或下滑
  - 松手后直接提取上一段或下一段文本
- [ ] 梳理该功能在当前实现中的状态机和数据来源
- [ ] 明确当前 `BoomActionHandler` / `BoomChipPage` 中哪些逻辑可直接复用
- [ ] 在现有 BigBang 页内补上上一段 / 下一段提取交互
- [ ] 做最小调试入口，避免只能靠悬浮球验证

#### 2.2 编辑模式

- [ ] 明确编辑模式的目标行为
  - 是否允许单词重新组合
  - 是否允许删除 / 合并 / 二次选择
  - 操作完成后如何回到普通 BigBang
- [ ] 先实现占位逻辑切换为真实状态切换
- [ ] 补 UI 反馈与退出路径

#### 2.3 原版交互补齐

- [ ] 补齐顶部 / 底部操作区的缺口行为
- [ ] 对齐选中态、清空态、空白点击、关闭逻辑
- [ ] 收口搜索 / 分享 / 复制等操作从 BigBang 到搜索页的过渡体验

### 验收标准

- [ ] “炸了又炸”可以真实使用
- [ ] 编辑模式不再只是占位按钮
- [ ] BigBang 主要交互不再依赖“先这样用”的临时兜底

## 目标三：三方应用适配与性能调优

### 目标

把当前“在部分场景可用”的主链路打磨到更稳定的日常可用状态，重点处理三方 App 兼容性和页面性能问题。

### 重点方向

#### 3.1 三方应用适配

- [ ] 无障碍文本提取命中率排查
- [ ] 悬浮球在不同 ROM / 多窗口 / 小窗模式下的行为检查
- [ ] BigBang / 搜索页在其他 App 上层显示时的返回栈和动画问题排查
- [ ] WebView 搜索页在常见站点的兼容性记录与修补
- [ ] 文本为空、文本过短、文本过长等边界场景处理

#### 3.2 性能调优

- [ ] 设置页 Compose 滚动与绘制性能继续收敛
- [ ] 大爆炸页外层 Compose 与内部 legacy View 的过绘检查
- [ ] 搜索页 WebView 启动时延与页面切换时延检查
- [ ] jieba 预热与首次分词耗时记录
- [ ] 悬浮球触发到 BigBang 展开的整链路耗时记录

#### 3.3 监测与验证

- [ ] 建立最小性能观察清单
  - 首次启动
  - 首次分词
  - 打开 BigBang
  - 打开搜索页
  - 设置页滚动
- [ ] 形成“问题机型 / 问题 App / 复现步骤”记录

### 验收标准

- [ ] 主流文本场景下悬浮球触发成功率可接受
- [ ] 设置页、大爆炸页、搜索页无明显掉帧和严重视觉 bug
- [ ] 三方 App 使用时返回栈、浮层显示、关闭逻辑基本稳定

## 当前不作为主目标的事项

以下内容不是不做，而是**不作为下一阶段主目标**：

- 全量 Kotlin 化 legacy Java 内核
- 完整 MVVM / ViewModel 分层重构
- 全量 Compose 重写所有 BigBang 内部词块逻辑
- 替换所有旧资源与动画实现
- 大规模目录重排

原因很简单：这些工作量大，但不直接补当前最缺的用户能力。

## 里程碑建议

### M1：OCR 交互补完第一轮

- 悬浮球可接入 OCR
- OCR 免框选策略有首版可用实现

### M2：原版核心 feature 补齐第一轮

- 炸了又炸可用
- 编辑模式可用

### M3：三方适配和性能第一轮收敛

- 悬浮球 / 无障碍 / 搜索页 / 设置页的主要体验问题收敛

## 执行原则

1. 继续保持“先让主链路可用，再补抽象”。
2. 不为了文档好看而把当前状态写成目标态。
3. 新功能优先接到现有可验证链路上，避免只加代码不加调试入口。
4. 对 legacy Java 内核做小步替换，不做一次性重写。
5. 性能问题优先找真实热区，不再为了视觉效果堆额外绘制层。
